server:
  port: 9090

spring:
  devtools:
    remote:
      restart:
        enabled: true

info:
  module-name: blacklabel-api
  log-file-name: app.log

# logging
logging.config: classpath:logging/logback-local.xml